package main.model;

import java.util.List;

public class SmmManager extends Employee{
    public SmmManager(String firstName, String lastName, double baseSalary, List<Double> bonus) {
        super(firstName, lastName, baseSalary, bonus);
    }
}
